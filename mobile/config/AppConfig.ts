const AppConfig = {
  // Default API URL for production (Fly.io)
  // This can be overridden in the app settings by the user
  API_BASE_URL: 'https://abdullah-junior-api.fly.dev',
  
  // Timeout for API requests in milliseconds
  API_TIMEOUT: 15000,
  
  // App version
  VERSION: '1.0.0',
};

export default AppConfig;
